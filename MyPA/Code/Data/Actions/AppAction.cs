namespace MyPA.Code
{
    public enum AppAction
    {
        DELETING_WORK_ITEM,
        INSERT_WORK_ITEM,
        APPLICATION_CLOSING, /* Notify models that the application is closing */
        MOVE_VERTICAL_SPLIT,
        CREATING_WORK_ITEM_JOURNAL_ENTRY,

    }
}
